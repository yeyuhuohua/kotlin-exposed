# Development only. No passwords or JWT signing secrets belong in the frontend.
API_PROXY_TARGET=http://127.0.0.1:8080
# Leave empty for same-origin deployment behind a reverse proxy.
VITE_API_BASE_URL=
